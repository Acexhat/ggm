import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
  root: {
    height: "5%",
    margin: "0.75rem",
    width:"50%",
    color:"#FFFF",
    display:"flex",
    justifyContent:"space-evenly",
    alignItems:"center",
    marginLeft:"25%"
  },
});
